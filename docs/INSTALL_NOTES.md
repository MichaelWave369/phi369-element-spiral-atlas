# Install Notes

This project uses Vite + React.

Standard local setup:

```bash
npm install
npm run dev
```

If `npm install` fails with a `403 Forbidden` from the npm registry in restricted environments,
that is typically a network, mirror, or policy issue rather than an application code issue.
Retry in a standard npm-enabled environment or with an approved registry mirror.
